/**
 * Twitter/X large summary card. Reuses the same generator as the OG
 * image so the share card stays identical across platforms.
 */
export { alt, size, contentType } from "./opengraph-image";
export { default } from "./opengraph-image";
